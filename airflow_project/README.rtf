First, you should have Docker Desktop installed. Then, open it 
until it says that is running.

From this folder, execute in the terminal:
docker compose up airflow-init

Once it ends, write:
docker compose up ("docker compose up -d" para dejarlo en segundo plano)

Now, open:variable
 http://localhost:8080

 with user: admin
    password: admin